# My Next Home - POC Environment File
# Copy this to .env.local and fill in values for your environment

# ============================================================================
# Database Configuration
# ============================================================================
# For local development with SQLite:
DATABASE_URL="file:./db.sqlite"

# For production with PostgreSQL (Supabase/Neon):
# DATABASE_URL="postgresql://user:password@host:5432/mynexthome"

# ============================================================================
# Integration Configuration
# ============================================================================
# Set to 'mock' for development (default), 'real' for production integrations
INTEGRATIONS_MODE=mock

# ============================================================================
# Real Integration Credentials (only needed if INTEGRATIONS_MODE=real)
# ============================================================================

# MLS/RESO Web API (for real listings provider)
# RESO_API_KEY=your_api_key_here
# RESO_API_BASE_URL=https://api.reso.org/v2

# Google Maps (for real commute time provider)
# GOOGLE_MAPS_API_KEY=AIza_your_key_here
# Note: Enable Distance Matrix API in Google Cloud Console

# Mapbox (alternative for commute time)
# MAPBOX_API_KEY=pk.eyJ_your_key_here

# GreatSchools (for real neighborhood school data, if available)
# GREATSCHOOLS_API_KEY=your_key_here

# ============================================================================
# Feature Flags
# ============================================================================
# Enable/disable real integrations (independent of INTEGRATIONS_MODE)
FEATURE_REAL_GEOCODING=false
FEATURE_REAL_COMMUTE=false
FEATURE_REAL_LISTINGS=false

# ============================================================================
# Application Configuration
# ============================================================================
# Public URL for the app (used in redirects, emails, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node environment
NODE_ENV=development

# ============================================================================
# Analytics & Monitoring (Future)
# ============================================================================
# SENTRY_DSN=https://your_sentry_key@sentry.io/project_id
# DATADOG_API_KEY=your_datadog_key
# ANALYTICS_ID=your_analytics_id

# ============================================================================
# Notes
# ============================================================================
# - INTEGRATIONS_MODE=mock uses seed data and deterministic mocks
# - INTEGRATIONS_MODE=real requires real API credentials (not yet implemented)
# - For Vercel deployment, set env vars in Vercel dashboard
# - Never commit .env files with real credentials
# - Use .env.local for local overrides (git ignored)
# - Use .env.production for production-specific values
